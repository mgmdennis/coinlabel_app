coinlabel_app — example project

Setup (backend):

```bash
cd backend
npm install
cp .env.example .env
# edit .env to set MONGODB_URI and PORT
npm start
```

Setup (frontend):

```bash
cd frontend
npm install
npm start
```
