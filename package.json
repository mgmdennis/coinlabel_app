{
  "name": "coinlabel-app",
  "version": "4.3.0",
  "description": "Coin labeling application",
  "main": "backend/server.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "node backend/server.js",
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install --include=dev && npm run build",
    "push": "node scripts/push.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cheerio": "^1.1.2",
    "express-session": "^1.19.0",
    "replicate": "^1.4.0",
    "sharp": "^0.34.5"
  }
}
